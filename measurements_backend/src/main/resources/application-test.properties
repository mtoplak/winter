spring.datasource.url=jdbc:mysql://localhost:3306/meastest
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
# to je vbistvu enako kot mysql properties, ampak je za testiranje

# pod @SpringBootTest
# napi?e? @ActiveProfiles("test")

# testi so v lo?enem folderju, da niso potem v produkciji, ko builda? in da lahko vse naenkrat za?ene? tukaj

# kode ne spreminjaj za testiranje